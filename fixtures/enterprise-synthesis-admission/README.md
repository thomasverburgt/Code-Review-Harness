# ENT-SYNTH Candidate Admission Fixtures

ADR-0024 admits ENT-SYNTH as an isolated, comparison-only candidate. The synthesis-input manifest binds exact domain artifacts, hashes, tiers, compatibility, freshness, authority ownership, and permitted use. Source-role hashes and contribution maps prevent synthesis prose from changing domain authority.

The ADR-0024 live GX-10 package intentionally mixes one accepted-live ENT-SYSRISK artifact with candidate-protocol ENT-ARCH, ENT-GOV, and ENT-STRAT artifacts. It proves mixed-tier handling only, not authoritative enterprise synthesis fitness.

ADR-0038 adds the exact accepted-live bounded domain set: accepted ENT-SYSRISK plus owner-finalized ENT-ARCH, ENT-GOV, and corrected ENT-STRAT eligibility. The gate excludes direct capability and child ingestion, carries active limitations and source context, preserves child-qualified human decision IDs, prohibits confidence averaging and invented correlations, and keeps report, distribution, scheduling, deployment, and production effects at none. The final Qwen3-32B GX-10 run passed, was accepted as semantically faithful, and is owner-finalized for later bounded shadow-integration evaluation only. Earlier timeout, projection-collision, duplicate-decision-ID, and missing-domain-limitation attempts remain retained as fail-closed development evidence.

Production targets the A100 large cluster; testing runs on DGX Spark or an approved equivalent.

Rollback restores ENT-SYNTH to `planned` and removes candidate discovery while retaining all evidence. It does not change baseline roles, candidate roles, reports, human records, or deployment state.
