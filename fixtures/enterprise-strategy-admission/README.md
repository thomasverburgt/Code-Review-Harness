# ENT-STRAT Candidate Admission Fixtures

This fixture set implements ADR-0023 without changing the accepted workflow. Human-controlled manifests are the sole sources of strategic objectives and scoring policy. The deterministic package proves scoring-contract, evidence-tier, missingness, sensitivity, and traceability mechanics; the live single-domain GX-10 run proves protocol and lineage handling only.

ADR-0037 adds an owner-declared, accepted-live, single-capability project-strategy evaluation using the exact semantically accepted CAP-SYNTH artifact. Authority registry version `0.8.0`, exact field locators, eligibility, objective, method, input, raw-response, and harness-projection provenance are bound. Human authority is observed; exact finding-locator coverage remains missing, so the objective remains `insufficient_evidence` with no imputation or composite. The project owner accepted it as-is with four explicitly carried limitations. It is eligible only for later ENT-SYNTH candidate evaluation and remains unscheduled.

The project owner later directed correction of those four limitations. The current corrected candidate uses registry `0.9.0`, scope `CODE-HARNESS-PROJECT-001`, explicit categorical mapping, declared confidence formulas, and observed-only distribution labeling. The prior accepted candidate and eligibility remain immutable history but are superseded for ADR-0038 input selection. The corrected candidate is semantically accepted, owner-finalized, and eligible only for the bounded ADR-0038 ENT-SYNTH candidate evaluation. Its active record carries the remaining evidence boundaries, unresolved CAP-SYNTH source context, human-owned promotion request, and locator-evidence remediation requirement without scheduling either agent.

The method intentionally prohibits a composite score. Missing measures remain unknown and no threshold, weight, or strategy is inferred. ENT-STRAT remains comparison-only and unscheduled.

Production targets the A100 large cluster. All testing runs on DGX Spark or an approved equivalent, currently GX-10. Rollback removes candidate discovery, restores ENT-STRAT to `planned`, and retains all evidence for audit without changing the accepted baseline, reports, ENT-ARCH, ENT-GOV, or deployment state.
