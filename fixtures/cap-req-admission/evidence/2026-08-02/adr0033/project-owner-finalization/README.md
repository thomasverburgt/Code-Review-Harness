# Rejected Generation Attempt

This uncommitted generation attempt is retained rather than rewritten or deleted. Its eligibility record inherited a deterministic `derived_at` value from ADR-0020 that predates the ADR-0033 project-owner finalization date. It is not an authoritative eligibility source and must not be consumed.

The corrected, separately identified package is under `../project-owner-finalization-corrected/` and is the only path referenced by the living current-state projection.
