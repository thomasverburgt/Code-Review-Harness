# Package Tests

This directory verifies the supported `code_harness` package and CLI boundary. The complete legacy regression suite remains under `tools` for the compatibility release and is run separately by CI.

