# Command Scripts

This directory is reserved for human-invoked build, calibration, export, and maintenance commands as implementation code migrates into `src/code_harness`.

During the compatibility release, existing commands remain under `tools`. New stable validation commands are exposed through the `harness-check` and `harness-validate` package entry points.

