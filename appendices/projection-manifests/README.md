# Projection Manifests

Projection manifests constrain the analytical fields exposed to each agent role. They preserve layer boundaries and prevent a model response from silently acquiring fields or authority outside its declared contract.

